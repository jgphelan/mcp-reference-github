# GitHub Personal Access Token
# Get one from: https://github.com/settings/tokens
#
# Required for WRITE operations (create issues, comment on issues)
# Required scopes: "repo" (private repos) or "public_repo" (public repos only)
#
# If no token is provided, the server runs in READ-ONLY mode
# Only public repository search and listing will work
GITHUB_TOKEN=your_github_token_here
